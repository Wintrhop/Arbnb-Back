const Reservation = require("./reservation.model")

module.exports = {
    async create (req,res) {
        try{
            const data = req.body

            
        } catch(err){

        }
    }
}